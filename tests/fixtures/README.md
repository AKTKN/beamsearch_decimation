# Controlled fixtures

correlated.dem is a hand-written converter-only model exercising repeat offsets,
separator correlations, XOR cancellation, distinct logical signatures and zero-prior
removal. It is not a physical benchmark. test_dem.py also exhausts a two-fault
physical circuit. Larger physical artifacts are saved under simulation_data/.
