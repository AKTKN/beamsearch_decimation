# Resolved hybrid manifest example

hybrid_manifest.json is the complete saved manifest from the Stage 6 single-worker
software check. It shows all resolved settings, source/build identities, table
versions, independent model sizes and execution metadata. Absolute paths and hashes
belong to that acceptance run; copy the YAML templates to configure another run.
The rate and four-shot counts are software checks, not production choices.
