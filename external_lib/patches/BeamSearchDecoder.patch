diff --git a/decoder/beam_search_decoder/__init__.pyi b/decoder/beam_search_decoder/__init__.pyi
index e625e8b..dade3b4 100644
--- a/decoder/beam_search_decoder/__init__.pyi
+++ b/decoder/beam_search_decoder/__init__.pyi
@@ -70,6 +70,10 @@ class BeamSearchDecoderBase:
             int: The number of iterations performed by the decoder.
         """
 
+    @property
+    def total_iterations(self) -> int:
+        """Completed initial and masked BP iterations in the most recent decode."""
+
 
     @property
     def check_count(self) -> int:
diff --git a/decoder/beam_search_decoder/_beam_search_decoder.pxd b/decoder/beam_search_decoder/_beam_search_decoder.pxd
index ea80b63..492d2b7 100644
--- a/decoder/beam_search_decoder/_beam_search_decoder.pxd
+++ b/decoder/beam_search_decoder/_beam_search_decoder.pxd
@@ -1,6 +1,7 @@
 #cython: language_level=3, boundscheck=False, wraparound=False, initializedcheck=False, cdivision=True, embedsignature=True
 # distutils: language = c++
 from libc.stdlib cimport malloc, calloc, free
+from libc.stdint cimport uint64_t
 from libcpp cimport bool
 from libcpp.vector cimport vector
 cimport numpy as np
@@ -50,6 +51,7 @@ cdef extern from "beam_search.hpp" namespace "ldpc::bp":
             vector[double] log_prob_ratios
             vector[double] initial_log_prob_ratios
             int iterations
+            uint64_t total_iterations
             bool converge
             vector[uint8_t] decode(vector[uint8_t]& syndrome)
 
diff --git a/decoder/beam_search_decoder/_beam_search_decoder.pyx b/decoder/beam_search_decoder/_beam_search_decoder.pyx
index 4aa3580..bd8a91e 100644
--- a/decoder/beam_search_decoder/_beam_search_decoder.pyx
+++ b/decoder/beam_search_decoder/_beam_search_decoder.pyx
@@ -216,6 +216,11 @@ cdef class BeamSearchDecoderBase:
         """
         return self.bpd.iterations
 
+    @property
+    def total_iterations(self) -> int:
+        """Completed initial and masked BP iterations in the most recent decode."""
+        return self.bpd.total_iterations
+
 
     @property
     def check_count(self) -> int:
@@ -438,6 +443,7 @@ cdef class BeamSearchDecoder(BeamSearchDecoderBase):
             if self._syndrome[i]: zero_input_vector = False
         if zero_input_vector:
             self.bpd.converge = True
+            self.bpd.total_iterations = 0
             return np.zeros(self.bit_count,dtype=DTYPE)
         self.bpd.decode(self._syndrome)
 
diff --git a/decoder/src_cpp/beam_search.hpp b/decoder/src_cpp/beam_search.hpp
index cc7d1a0..790c22d 100644
--- a/decoder/src_cpp/beam_search.hpp
+++ b/decoder/src_cpp/beam_search.hpp
@@ -60,6 +60,7 @@ namespace ldpc {
             std::vector<double> log_prob_ratios;
             std::vector<double> initial_log_prob_ratios;
             int iterations;
+            uint64_t total_iterations = 0; // Completed BP steps across every path in this decode.
             bool converge;
 
             BeamSearchDecoder(
@@ -106,6 +107,7 @@ namespace ldpc {
             std::vector<uint8_t> &decode(std::vector<uint8_t> &syndrome) {
 
                 this->converge = 0;
+                this->total_iterations = 0;
 
                 this->initialise_log_domain_bp();
 
@@ -210,6 +212,7 @@ namespace ldpc {
                     }
 
                     this->iterations = it;
+                    ++this->total_iterations;
 
                     if (this->converge) {
                         min_dec_weight = 0;
@@ -375,6 +378,7 @@ namespace ldpc {
                             }
 
                             this->iterations = it;
+                            ++this->total_iterations;
 
                             if (this->converge) {
                                 cur_dec_weight = 0;
