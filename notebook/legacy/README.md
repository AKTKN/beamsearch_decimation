# Preserved consumer notebook

`benchmark_analysis.ipynb.example` preserves the pre-migration notebook structure,
with calls redirected to `analysis.legacy`. It expects a full benchmark YAML.
Execute it through scripts/legacy/execute_notebook.sh using search_decimation.
Existing user notebooks and archived executed notebooks are left untouched.
